# Review 报告 v2 — 回归验证（基于 v1 补丁后）

> 日期：2026-08-21 | Round 2/5 | 输入：v1 补丁后的 Spec (e3ae1cd) + Plan (e3ae1cd)

## 1. 执行摘要

- **总体结论：** v1 提出的 6 项高/中风险已在 Spec §2.2/§4.2/§5.4/§6/§7/§11 与 Plan Task1/3/5/9/10 中闭环，本轮**无新增不一致**。
- **变更判定：** 本轮**0 项需改动**，计为第 **1 次连续无变化**（需 2 次才停止）。
- **验证方式：** 逐项回检 + 代码基线复核（`CommandLineParser.normalizeConfig` 已确认为 private，需 Task1 落地时提升为 public，属 Plan 内执行项，不算 Spec 缺口）。

## 2. 回归对照

| v1 风险 | 修复位置 | 本轮复核 | 状态 |
|---------|----------|----------|------|
| R1 sidecar 形态 | Spec §7 双路径 + Plan Task10 dev/release 分支 | dev 用 `shell spawn node`，release 用 `externalBin` + SEA，逻辑自洽 | ✅ 已闭环 |
| R2 better-sqlite3 原生 | Spec §7/§11 + Plan Task5 备注 external/resources | 明确 `resources` 附带 `node_modules/better-sqlite3`，与 Tauri 官方 `resources` 语义一致 | ✅ 已闭环 |
| R3 normalizeConfig 分裂 | Spec §4.2 补归一化约束 + Plan Task3 调 `normalizeConfig` | `privateKey` 文件选择器 + `~/` 展开 + POSIX 校验均已写入，测试用例需覆盖 `~/id_rsa` | ✅ 已闭环 |
| R4 双端口冲突 | Spec §2.2 + Plan Global Constraints 补共存说明 | `--port` vs `--admin-port` 语义分离，Help 分段待 Plan Task2 落地时更新 | ✅ 已闭环 |
| R5 注册 scope | Spec §5.4 新增契约 + Plan Task9 补 conflict | `scope=user/project/local` + `conflict` 弹窗已定义，Cursor 仅 user 已注明 | ✅ 已闭环 |
| R6 AntD 锁定 | Spec §6 删“或 shadcn” | 锁定 Ant Design 5 | ✅ 已闭环 |

**代码基线二次核对（未新增风险）：**
- `src/models/types.ts:SSHConfig` 的 `algorithms/keepalive/commandTemplate` 等字段在 Plan Task1 的 `ConnectionSchema.passthrough()` 下可透传，归一化时由 `normalizeConfig` 处理，无需前端逐字段 zod 精确建模。
- `src/services/ssh-connection-manager.ts` 的 `shell` 模式与 `exec` 的 `allowedRemotePaths` 校验差异已在 Spec §8 保留，未新增 gap。
- `scripts/build.js` 的 `tsc` 单步与新增 `scripts/build-sidecar.js` 的 `esbuild` 单文件不冲突，Plan Task10 的 `beforeBuildCommand` 已排序。

## 3. 不确认清单（v1 的 5 项）

1. 打包附带 Node+node_modules — 已由 Spec §7 双路径说明，用户默接受（Round1 已确认“都接受”）。
2. `normalizeConfig` 提升为 public — Plan 内执行项，无需再确认。
3. 双端口共存 — 已写入 Spec，无需再确认。
4. `local` scope — 已写入 §5.4，无需再确认。
5. AntD — 已锁定，无需再确认。

→ **本轮无待确认项残留**。

## 4. 本轮拟改动

- **0 项**。Spec/Plan 无需再改。

## 5. 下一步

- 进入 Round 3，若仍 0 改动则达成“连续2次无变化”停止条件。
